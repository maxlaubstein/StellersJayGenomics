Running admixture on the 306 non-Mesoamerican samples (from 3 different seeds). 

```makeinput.sh``` is used to convert vcf into format needed for admixture input. Run that first.

There is a different directory for each different seed, each containing a shell script ```run.sh``` that will launch admixture. I ran these sort of in parallel by running them each in different screen sessions.
